mutation InsertHabit($userId: Int!, $name: String!, $time: timestamptz, $habitType: habit_type_enum!) {
	insert_habit_one(object: { user_id: $userId, name: $name, habit_type: $habitType }) {
		habit_id
		name
		created_at
		habit_type
		time_created
		user_id
	}
}
